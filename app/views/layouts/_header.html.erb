<div class= "header">
    <nav class="top-bar" data-topbar role="navigation">
      <ul class="title-area">
        <li class="name">
          <h1><%= link_to "Simulation Game!", dashboard_path %></h1>
        </li>
      </ul>

      <section class="top-bar-section">
        <!-- Right Nav Section -->
        <ul class="right">
          <li>
            <% if current_user %>
              <%= link_to "Verwaltung", management_path %>
            <% end %>
          </li>
          <li>
            <% if current_user %>
              <%= link_to "Handel", trades_path %>
            <% end %>
          </li>
        </ul>

      </section>
    </nav>
</div>
